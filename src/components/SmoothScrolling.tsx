'use client';

import { useEffect } from 'react';

const SmoothScrolling = ({ children }: { children: React.ReactNode }) => {
  // Smooth scrolling disabled for better performance
  // Using native browser scroll instead
  
  return <>{children}</>;
};

export default SmoothScrolling; 